"""Versioned, platform-owned browser runtime and theme."""

RENDERER_VERSION = "2.0"
RUNTIME_VERSION = "2.0"
ALLOWED_SCENE_KINDS = {"intro", "explain", "example", "compare", "practice", "scenario", "quiz", "recap"}

STYLE = """
:root{color-scheme:light;font-family:var(--font-body,system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif);color:var(--ink,#172033);background:var(--canvas,#f4f8fc);font-size:var(--font-size-body,1rem)}*{box-sizing:border-box}body{margin:0;line-height:1.65}.course{max-width:var(--content-max,60rem);margin:auto;padding:var(--space-page,24px)}.course-header{background:var(--accent,#123b68);color:var(--surface,#fff);padding:var(--space-card,24px);border-radius:var(--radius-card,14px)}.course-header h1{margin:0;font-size:var(--font-size-title,2rem)}.course-header p{margin:6px 0 0}.progress{margin:16px 0;color:var(--ink,#36516e)}.scene{display:none;background:var(--surface,#fff);padding:var(--space-card,26px);border-radius:var(--radius-card,14px);box-shadow:0 4px 18px #17324b16}.scene.active{display:block}.scene h2{margin-top:0}.block{white-space:pre-wrap}.steps{padding-left:1.25rem}.steps li{margin:10px 0}.quiz-option{display:block;padding:10px;margin:8px 0;border:1px solid var(--border,#bfd0e2);border-radius:var(--radius-control,8px);cursor:pointer;min-height:var(--touch-min,44px)}.feedback{padding:10px;border-radius:var(--radius-control,8px);background:color-mix(in srgb,var(--accent,#123b68) 10%,var(--surface,#fff))}.nav{display:flex;gap:10px;justify-content:space-between;margin-top:20px}.nav button,.check{font:inherit;padding:9px 14px;min-height:var(--touch-min,44px);border-radius:var(--radius-control,8px);border:1px solid var(--accent,#1d5b97);background:var(--surface,#fff);color:var(--accent,#123b68);cursor:pointer}.nav button:focus-visible,.check:focus-visible,input:focus-visible{outline:var(--focus-width,3px) solid var(--focus,#e69b26);outline-offset:3px}@media(max-width:640px){.course{padding:12px}.scene{padding:18px}.nav button{min-height:44px}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;animation-duration:0.01ms!important;animation-iteration-count:1!important;transition-duration:0.01ms!important}}
""".strip() + """
/* Platform-owned layout semantics. */
.course{transition:background .2s ease, color .2s ease}
.layout-cover .course-header{min-height:clamp(180px,32vh,320px);display:flex;flex-direction:column;justify-content:center}
.layout-chapter .scene{border-inline-start:6px solid var(--accent)}
.layout-focus .scene{max-width:46rem;margin-inline:auto}
.layout-steps .scene{counter-reset:course-step;border-block-start:4px solid var(--accent)}
.layout-compare .scene{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:var(--space-card)}
.layout-steps .scene .component-steps,.layout-practice .scene .component-steps{max-width:44rem;margin-inline:auto}
.layout-recap .scene{border-top:8px solid var(--accent)}
.layout-progress .scene{background:linear-gradient(135deg,var(--surface),color-mix(in srgb,var(--accent) 8%,var(--surface)))}
.component-callout,.component-key-point,.component-compare,.component-recap{padding:var(--space-card);margin-block:var(--space-card);border:1px solid var(--border);border-radius:var(--radius-card);box-shadow:var(--shadow-card)}
.component-steps label,.component-ordered-steps label,.component-choice label{display:flex;align-items:center;gap:.6rem;min-height:var(--touch-min);padding:.5rem;border-radius:var(--radius-control)}
.component-choice{border:1px solid var(--border);border-radius:var(--radius-card);padding:var(--space-card)}
.component-flashcard,.component-matching,.component-ordering{padding:var(--space-card);margin-block:var(--space-card);border:1px solid var(--border);border-radius:var(--radius-card)}.component-flashcard button,.component-matching button,.component-ordering button{min-height:var(--touch-min);padding:.55rem .75rem;border:1px solid var(--border);border-radius:var(--radius-control);background:var(--surface);color:var(--ink);cursor:pointer}.component-matching{display:grid;gap:.75rem}.matching-left,.matching-right{display:flex;flex-wrap:wrap;gap:.5rem}.component-matching button[data-selected="true"]{outline:var(--focus-width,3px) solid var(--focus)}.component-matching button[data-matched="true"]{background:color-mix(in srgb,var(--accent) 18%,var(--surface))}.component-ordering li{display:flex;align-items:center;gap:.5rem;margin:.5rem 0}.component-ordering li button{min-width:44px}.component-ordering li span{flex:1}
.component-branching-scenario,.component-categorization,.component-word-bank-cloze,.component-timeline-explorer{padding:var(--space-card);margin-block:var(--space-card);border:1px solid var(--border);border-radius:var(--radius-card)}.component-branching-scenario button,.component-categorization button,.component-word-bank-cloze button,.component-timeline-explorer button{min-height:var(--touch-min);padding:.55rem .75rem;border:1px solid var(--border);border-radius:var(--radius-control);background:var(--surface);color:var(--ink);cursor:pointer;margin:.25rem}.component-branching-scenario [data-branch-node]:not([hidden]){display:grid;gap:.5rem}.component-word-bank-cloze p:first-of-type{display:flex;flex-wrap:wrap;align-items:center;gap:.25rem}.component-timeline-explorer .timeline-events{display:flex;flex-wrap:wrap;gap:.5rem}
@keyframes courseware-enter{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
[data-motion="subtle"] .scene.active{animation:courseware-enter .22s ease-out}
[data-motion="reduced"] *{animation:none!important;transition:none!important;scroll-behavior:auto!important}
@media(prefers-reduced-motion:reduce){*,*::before,*::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
@media(forced-colors:active){.component-flashcard,.component-matching,.component-ordering,.component-callout,.component-key-point,.component-compare,.component-recap,.component-choice{forced-color-adjust:auto;border:1px solid CanvasText;background:Canvas;color:CanvasText}.nav button,.check,.component-flashcard button,.component-matching button,.component-ordering button{border:1px solid ButtonText;background:ButtonFace;color:ButtonText}}
@media(max-width:640px){.layout-compare .scene{display:block}.layout-compare .scene>*+*{margin-top:var(--space-card)}}
""".strip()

# Design system 2.0: the desktop runtime is a fixed, single-page stage.  The
# mobile breakpoint deliberately returns to document flow for readable reflow.
STYLE += """
html,body{width:100%;height:100%;overflow:hidden}body{background:var(--canvas);font-size:clamp(16px,1.05vw,19px)}
.course{width:100%;height:100dvh;max-width:none;margin:0;padding:var(--safe-area);display:grid;grid-template-rows:auto auto minmax(0,1fr) auto;gap:clamp(.5rem,1.2vh,1rem);overflow:hidden;background:radial-gradient(circle at 92% 8%,var(--accent-soft) 0,transparent 26%),var(--canvas)}.course .scene{max-width:none!important;margin-inline:0!important}
.course-topbar{display:flex;align-items:center;justify-content:space-between;gap:1rem;min-height:var(--page-header-height);padding-inline:.25rem}.course-topbar h1{margin:0;font-size:clamp(1rem,1.4vw,1.35rem);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.course-topbar span,.scene-kicker{color:var(--muted);font-size:.78rem;font-weight:750;letter-spacing:.08em;text-transform:uppercase}.progress{margin:0;color:var(--muted);font-size:.84rem}.course-stage{min-height:0;overflow:hidden}.scene{width:100%;height:100%;min-height:0;margin:0;padding:clamp(1.15rem,2.4vw,2.4rem);overflow:hidden;border:1px solid var(--border);border-radius:var(--radius-panel);box-shadow:var(--shadow-floating);background:var(--surface)}.scene:not(.active){display:none!important}.scene.active{display:grid;grid-template-rows:auto minmax(0,1fr) auto;gap:clamp(.65rem,1.5vh,1.2rem)}.scene-header{display:grid;gap:.35rem}.scene-header h2{margin:0;font-size:clamp(1.45rem,2.65vw,2.45rem);line-height:1.12}.scene-question,.scene-lead{margin:0;color:var(--muted)}.scene-question{font-weight:650}.scene-body{min-height:0;display:grid;grid-template-columns:repeat(12,minmax(0,1fr));grid-auto-rows:minmax(0,1fr);gap:clamp(.65rem,1.3vw,1rem);align-content:stretch;overflow:hidden}.scene-body>.block,.scene-body>section,.scene-body>aside,.scene-body>fieldset,.scene-body>.steps,.scene-body>[data-quiz]{grid-column:span 6;min-width:0;min-height:0;margin:0;overflow:hidden}.scene-body>:only-child{grid-column:1/-1}.scene-conclusion{margin:0;padding:.7rem 1rem;border-inline-start:4px solid var(--accent);border-radius:var(--radius-control);background:var(--accent-soft);font-weight:650}.nav{min-height:var(--page-footer-height);margin:0;display:grid;grid-template-columns:1fr auto 1fr;align-items:center}.nav button:first-child{justify-self:start}.nav button:last-child{justify-self:end}.nav-status{color:var(--muted);font-size:.82rem}.nav button:disabled{opacity:.38;cursor:not-allowed}
.course.is-cover .course-topbar,.course.is-cover>.progress{display:none}.course.is-cover{grid-template-rows:0 0 minmax(0,1fr) auto}
.course-topbar{grid-row:1}.progress{grid-row:2}.course-stage{grid-row:3;align-self:stretch;height:auto}.nav{grid-row:4}
.recipe-editorial_cover{color:#fff;background:linear-gradient(125deg,var(--accent),color-mix(in srgb,var(--accent) 72%,#071a2f));border:0}.recipe-editorial_cover .scene-kicker,.recipe-editorial_cover .scene-question,.recipe-editorial_cover .scene-lead{color:#dcecff}.recipe-editorial_cover .scene-header h2{font-size:clamp(2.2rem,5vw,5rem);max-width:18ch}.recipe-editorial_cover .scene-body>*{background:#ffffff14;border-color:#ffffff40;color:#fff}.recipe-learning_map_grid .scene-body>*{grid-column:span 4}.recipe-process_lane .scene-body{grid-template-columns:repeat(4,minmax(0,1fr))}.recipe-process_lane .scene-body>*{grid-column:span 1}.recipe-comparison_matrix .scene-body>*{grid-column:span 6}.recipe-case_diagnostic .scene-body>*:first-child{grid-column:span 5}.recipe-case_diagnostic .scene-body>*:nth-child(2){grid-column:span 7}.recipe-case_diagnostic .scene-body>*:nth-child(n+3){grid-column:span 6}.recipe-practice_workspace .scene-body>*:first-child{grid-column:span 7}.recipe-practice_workspace .scene-body>*:nth-child(n+2){grid-column:span 5}.recipe-quiz_focus .scene-body{grid-template-columns:repeat(12,minmax(0,1fr))}.recipe-quiz_focus .scene-body>*{grid-column:span 6}.recipe-quiz_focus .scene-body>[data-quiz],.recipe-quiz_focus .scene-body>.component-choice{grid-column:1/-1}.recipe-recap_dashboard .scene-body>*{grid-column:span 4}
.recipe-practice_workspace{background:linear-gradient(135deg,var(--surface),color-mix(in srgb,var(--accent) 11%,var(--surface)));border-inline-start:clamp(5px,.55vw,9px) solid var(--data-2)}.recipe-practice_workspace .scene-header{display:grid;grid-template-columns:auto 1fr;column-gap:1rem;align-items:start}.recipe-practice_workspace .scene-kicker{grid-row:1/3;writing-mode:vertical-rl;color:var(--data-2);font-weight:800;letter-spacing:.12em}.recipe-practice_workspace .component-steps{background:color-mix(in srgb,var(--data-2) 10%,var(--surface));border:1px dashed var(--data-2);border-radius:var(--radius-card);padding:1rem}.recipe-practice_workspace .component-steps li{list-style:none}.recipe-practice_workspace .component-steps label:has(input:checked){background:color-mix(in srgb,var(--data-2) 18%,var(--surface));text-decoration:line-through}
.recipe-process_lane{background:linear-gradient(180deg,color-mix(in srgb,var(--data-1) 10%,var(--surface)),var(--surface));border-top:5px solid var(--data-1)}.recipe-process_lane .scene-body{counter-reset:step-card}.recipe-process_lane .scene-body>*{position:relative;padding-top:2.4rem}.recipe-process_lane .scene-body>*::before{counter-increment:step-card;content:"0" counter(step-card);position:absolute;top:.7rem;left:.8rem;color:var(--data-1);font-weight:900;font-size:1.25rem}.recipe-process_lane .component-steps{grid-column:span 2;background:var(--surface-alt)}
.recipe-concept_split{background:linear-gradient(115deg,var(--surface) 0 62%,color-mix(in srgb,var(--data-3) 12%,var(--surface)) 62%)}.recipe-concept_split .scene-header{max-width:58ch}.recipe-concept_split .scene-body>*:first-child{grid-column:span 7;border-inline-start:5px solid var(--data-3)}.recipe-concept_split .scene-body>*:nth-child(n+2){grid-column:span 5}.recipe-concept_split .component-steps{background:color-mix(in srgb,var(--data-3) 12%,var(--surface))}
@media(max-width:768px){html,body{height:auto;min-height:100%;overflow-x:hidden;overflow-y:auto}.course{height:auto;min-height:100dvh;display:block;padding:.75rem;overflow:visible}.course-topbar,.progress,.course-stage,.nav{margin-bottom:.75rem}.course-stage{overflow:visible}.scene{height:auto;min-height:calc(100dvh - 9rem);overflow:visible}.scene.active{display:block}.scene-body{display:block;overflow:visible}.scene-body>*{margin-block:.75rem!important;overflow:visible!important}.nav{position:sticky;bottom:.5rem;display:grid;padding:.5rem;border:1px solid var(--border);border-radius:.8rem;background:color-mix(in srgb,var(--surface) 92%,transparent);backdrop-filter:blur(10px)}}
@media(max-height:720px) and (min-width:769px){.course{--safe-area:.75rem;gap:.4rem}.scene{padding:1rem}.scene-header h2{font-size:1.55rem}.scene-body{gap:.55rem}.component-callout,.component-key-point,.component-compare,.component-recap{padding:.75rem;font-size:.9rem}}
.component-metric-strip,.component-process-flow,.component-concept-map,.component-evidence-card,.component-comparison-table,.component-decision-path,.component-code-steps,.component-conclusion-bar{padding:clamp(.75rem,1.4vw,1.15rem);border:1px solid var(--border);border-radius:var(--radius-card);background:var(--panel)}.visual-items{display:grid;grid-template-columns:repeat(auto-fit,minmax(8rem,1fr));gap:.6rem;margin:.7rem 0 0;padding:0;list-style:none}.visual-items li{display:grid;gap:.25rem;padding:.65rem;border-radius:var(--radius-control);background:var(--surface-alt)}.visual-items span{color:var(--muted);font-size:.78rem}.visual-items strong{color:var(--accent);font-size:1.05rem}.component-process-flow .visual-items,.component-decision-path .visual-items{counter-reset:visual-step}.component-process-flow .visual-items li,.component-decision-path .visual-items li{position:relative;border-inline-start:3px solid var(--data-2)}.component-comparison-table table{width:100%;border-collapse:collapse}.component-comparison-table th,.component-comparison-table td{padding:.55rem;text-align:start;border-bottom:1px solid var(--border)}.component-code-steps{font-family:var(--font-mono);background:color-mix(in srgb,var(--ink) 5%,var(--surface))}.component-conclusion-bar{border-inline-start:6px solid var(--data-2);background:var(--accent-soft)}
/* Detailed practice pages reserve the dominant area for source explanation
   and code. Brief progress/check hints stay compact and never cover it. */
.recipe-practice_workspace .scene-body{grid-template-columns:repeat(12,minmax(0,1fr));grid-template-rows:auto minmax(0,1fr) auto;gap:clamp(.55rem,1vw,.9rem)}
.recipe-practice_workspace .scene-body>.component-steps{grid-column:span 3;grid-row:1/4;align-self:start;max-height:100%;overflow:auto;margin:0}
.recipe-practice_workspace .scene-body>.component-key-point{grid-column:4/13;grid-row:1/3;display:flex;min-height:0;padding:clamp(.8rem,1.4vw,1.2rem);margin:0;overflow:visible}
.recipe-practice_workspace .scene-body>.component-callout:nth-child(3){grid-column:4/9;grid-row:3;min-height:0;padding:.7rem .9rem;margin:0;overflow:visible}
.recipe-practice_workspace .scene-body>.component-callout:nth-child(4){grid-column:9/13;grid-row:3;min-height:0;padding:.7rem .9rem;margin:0;overflow:visible}
.recipe-practice_workspace .component-content{min-height:0;overflow:visible;width:100%}.component-prose{white-space:pre-wrap;overflow-wrap:anywhere;margin:.15rem 0 .75rem}.source-code{display:block;max-height:none;margin:.7rem 0 0;padding:.6rem .75rem;overflow:visible;white-space:pre-wrap;overflow-wrap:anywhere;font-family:var(--font-mono);font-size:clamp(.54rem,.62vw,.78rem);line-height:1.3;color:var(--ink);background:color-mix(in srgb,var(--ink) 7%,var(--surface));border:1px solid var(--border);border-radius:var(--radius-control);tab-size:2}.source-code:focus-visible{outline:var(--focus-width,3px) solid var(--focus);outline-offset:2px}
.recipe-practice_workspace[data-practice-variant="code"]{border-inline-start-color:var(--data-1);background:linear-gradient(135deg,color-mix(in srgb,var(--data-1) 9%,var(--surface)),var(--surface))}.recipe-practice_workspace[data-practice-variant="code"] .scene-kicker{color:var(--data-1)}.recipe-practice_workspace[data-practice-variant="code"] .scene-body>.component-steps{grid-column:1/13;grid-row:1;max-height:5.2rem;padding:.55rem .85rem}.recipe-practice_workspace[data-practice-variant="code"] .scene-body>.component-key-point{grid-column:1/13;grid-row:2;}.recipe-practice_workspace[data-practice-variant="code"] .scene-body>.component-callout:nth-child(3){grid-column:1/7;grid-row:3}.recipe-practice_workspace[data-practice-variant="code"] .scene-body>.component-callout:nth-child(4){grid-column:7/13;grid-row:3}.recipe-practice_workspace[data-practice-variant="code"] .source-code{background:color-mix(in srgb,var(--data-1) 7%,var(--surface))}
.recipe-practice_workspace[data-practice-variant="verify"]{border-inline-start-color:var(--data-3);background:linear-gradient(135deg,var(--surface),color-mix(in srgb,var(--data-3) 10%,var(--surface)))}.recipe-practice_workspace[data-practice-variant="verify"] .scene-kicker{color:var(--data-3)}.recipe-practice_workspace[data-practice-variant="verify"] .scene-body>.component-steps{grid-column:10/13;grid-row:1/4}.recipe-practice_workspace[data-practice-variant="verify"] .scene-body>.component-key-point{grid-column:1/10;grid-row:1/3}.recipe-practice_workspace[data-practice-variant="verify"] .scene-body>.component-callout:nth-child(3){grid-column:1/6;grid-row:3}.recipe-practice_workspace[data-practice-variant="verify"] .scene-body>.component-callout:nth-child(4){grid-column:6/10;grid-row:3}
/* The operation title belongs in one stable horizontal header.  The former
   vertical kicker created a large unused upper-left area on practice pages. */
.recipe-practice_workspace .scene-header{display:block}.recipe-practice_workspace .scene-kicker{display:block;writing-mode:horizontal-tb;margin:0 0 .2rem;color:var(--data-2);font-size:.68rem;letter-spacing:.1em}.recipe-practice_workspace .scene-header h2{max-width:none;font-size:clamp(1.45rem,2.2vw,2.15rem);line-height:1.12}.recipe-practice_workspace .scene-question{display:none}.recipe-practice_workspace .scene-lead{display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:1;overflow:hidden;font-size:.82rem;line-height:1.35}
/* Hints support the step; they must never compete with its explanation. */
.recipe-practice_workspace .component-callout{font-size:clamp(.7rem,.78vw,.84rem);line-height:1.38}.recipe-practice_workspace .component-callout strong{font-size:.8rem}.recipe-practice_workspace .component-callout .component-prose{margin:.1rem 0}.recipe-practice_workspace .component-steps{font-size:clamp(.72rem,.82vw,.9rem);line-height:1.35}.recipe-practice_workspace .component-steps label{padding:.35rem .45rem;min-height:2.35rem}
/* Guided pages use a compact action bar so the detailed operation gets the
   full central canvas. Code and verification retain distinct arrangements. */
.recipe-practice_workspace[data-practice-variant="guided"] .scene-body>.component-steps{grid-column:1/13;grid-row:1;max-height:4.4rem;padding:.45rem .75rem}.recipe-practice_workspace[data-practice-variant="guided"] .scene-body>.component-key-point{grid-column:1/13;grid-row:2}.recipe-practice_workspace[data-practice-variant="guided"] .scene-body>.component-callout:nth-child(3){grid-column:1/7;grid-row:3}.recipe-practice_workspace[data-practice-variant="guided"] .scene-body>.component-callout:nth-child(4){grid-column:7/13;grid-row:3}
.recipe-practice_workspace[data-practice-variant="verify"] .scene-body>.component-steps{align-self:start;max-height:5rem;padding:.5rem .6rem}.recipe-practice_workspace[data-practice-variant="verify"] .component-callout{font-size:.74rem}
@media(max-height:720px) and (min-width:769px){.recipe-practice_workspace .scene-header h2{font-size:1.5rem}.recipe-practice_workspace .scene-body>.component-steps{padding:.65rem}.recipe-practice_workspace .scene-body>.component-callout:nth-child(3),.recipe-practice_workspace .scene-body>.component-callout:nth-child(4){font-size:.82rem;padding:.55rem .7rem}.source-code{font-size:.64rem;padding:.55rem .65rem}}
""".strip()

SCRIPT = """
(()=>{
  let i=0,nonce='',parentOrigin='';
  const root=document.querySelector('.course'),scenes=[...document.querySelectorAll('.scene')],progress=document.querySelector('.progress');
  const context={resource_id:root?.dataset.resourceId||'unknown-resource',release_id:root?.dataset.releaseId||'unknown-release'};
  const safeState=state=>{const result=Object.fromEntries(Object.entries(state||{}).filter(([key])=>['scene_index','scene_count','correct','completed','attempt','duration_ms'].includes(key)));const source=state?.component_state||{},component_state={};if(['front','back','known','review'].includes(source.flashcard?.status))component_state.flashcard={status:source.flashcard.status};if(source.matching&&Array.isArray(source.matching.matched_pair_ids)&&source.matching.matched_pair_ids.every(item=>typeof item==='string'))component_state.matching={matched_pair_ids:[...new Set(source.matching.matched_pair_ids)].slice(0,16),...(typeof source.matching.completed==='boolean'?{completed:source.matching.completed}:{})};if(source.ordering&&Array.isArray(source.ordering.order)&&source.ordering.order.every(item=>typeof item==='string'))component_state.ordering={order:[...new Set(source.ordering.order)].slice(0,16),...(typeof source.ordering.submitted==='boolean'?{submitted:source.ordering.submitted}:{}),...(typeof source.ordering.correct==='boolean'?{correct:source.ordering.correct}:{})};if(Object.keys(component_state).length)result.component_state=component_state;return result};
  const send=(type,extra={})=>{if(parentOrigin)parent.postMessage({type,nonce,...extra},parentOrigin)};
  const emit=event=>{const payload={...event,...context,state:safeState(event.state)};payload.occurrence_id=`runtime:${Date.now()}:${Math.random().toString(16).slice(2)}`;payload.event_id=payload.occurrence_id;try{const key='zhiyu.courseware.learning-events.v1';const queued=JSON.parse(localStorage.getItem(key)||'[]');queued.push(payload);localStorage.setItem(key,JSON.stringify(queued))}catch(_){};if(parentOrigin)parent.postMessage({type:'learning_event',nonce,event:payload},parentOrigin)};
  const sceneId=node=>node?.closest('.scene')?.dataset.sceneId||`scene-${i}`;
  const show=n=>{i=Math.max(0,Math.min(n,Math.max(0,scenes.length-1)));scenes.forEach((s,x)=>s.classList.toggle('active',x===i));root?.classList.toggle('is-cover',scenes[i]?.dataset.pageRole==='cover');const nav=[...document.querySelectorAll('[data-nav]')];if(nav[0])nav[0].disabled=i===0;if(nav[1])nav[1].disabled=i===scenes.length-1;if(progress)progress.textContent=`第 ${i+1} / ${scenes.length} 页`;if(scenes[i])emit({event_type:'scene_viewed',scene_id:sceneId(scenes[i]),state:{scene_index:i,scene_count:scenes.length}});send('progress',{scene_index:i,scene_count:scenes.length})};
  const applyRestore=restore=>{if(!restore||typeof restore!=='object')return;const targetId=typeof restore.current_scene_id==='string'?restore.current_scene_id:'';const targetIndex=Number.isInteger(restore.current_scene_index)?restore.current_scene_index:scenes.findIndex(scene=>scene.dataset.sceneId===targetId);if(targetIndex>=0)show(targetIndex);const state=restore.component_state||{},flash=state.flashcard?.status;if(['front','back','known','review'].includes(flash))document.querySelectorAll('[data-flashcard]').forEach(card=>{const front=card.querySelector('.flash-front'),back=card.querySelector('.flash-back');if(front)front.hidden=flash==='back';if(back)back.hidden=flash!=='back';card.dataset.reviewStatus=flash});if(state.ordering?.order)document.querySelectorAll('[data-ordering]').forEach(box=>{const list=box.querySelector('ol'),byId=new Map([...box.querySelectorAll('li')].map(item=>[item.dataset.itemId,item]));state.ordering.order.forEach(id=>{const item=byId.get(id);if(item)list.appendChild(item)})});if(state.matching?.matched_pair_ids)document.querySelectorAll('[data-matching] [data-pair-id]').forEach(button=>{button.dataset.matched=String(state.matching.matched_pair_ids.includes(button.dataset.pairId))})};
  const reset=()=>{i=0;document.querySelectorAll('[data-quiz] input').forEach(input=>{input.checked=false});document.querySelectorAll('.feedback').forEach(feedback=>{feedback.hidden=true;feedback.textContent=''});document.querySelectorAll('[data-flashcard]').forEach(card=>{const front=card.querySelector('.flash-front'),back=card.querySelector('.flash-back');if(front)front.hidden=false;if(back)back.hidden=true});document.querySelectorAll('[data-matching] [data-match]').forEach(button=>{button.dataset.selected='false';button.dataset.matched='false'});document.querySelectorAll('[data-ordering]').forEach(box=>{const list=box.querySelector('ol');const initial=[...box.querySelectorAll('li')].sort((a,b)=>Number(a.dataset.initialOrder)-Number(b.dataset.initialOrder));if(list)initial.forEach(item=>list.appendChild(item))});show(0);window.scrollTo({top:0,behavior:'auto'});send('restart',{scene_index:0,scene_count:scenes.length})};
  document.querySelectorAll('[data-ordering] li').forEach((item,index)=>{item.dataset.initialOrder=String(index);item.dataset.itemId=item.dataset.itemId||`item-${index}`});
  document.addEventListener('click',e=>{const n=e.target.closest('[data-nav]');if(n)return show(i+Number(n.dataset.nav));const quiz=e.target.closest('[data-quiz]');if(quiz){const selected=[...quiz.querySelectorAll('input:checked')].map(x=>x.value).sort().join('|'),correct=selected===quiz.dataset.answer,feedback=quiz.querySelector('.feedback');if(feedback){feedback.hidden=false;feedback.textContent=correct?'回答正确。':(quiz.dataset.feedback||'请再回顾相关内容后重试。')}emit({event_type:'answer_submitted',scene_id:sceneId(quiz),state:{correct}});emit({event_type:correct?'answer_correct':'answer_retry',scene_id:sceneId(quiz),state:{correct}});send('quiz_result',{correct});return}const flash=e.target.closest('[data-flashcard]');if(flash){const action=e.target.dataset.flashcardAction,back=flash.querySelector('.flash-back'),front=flash.querySelector('.flash-front');if(action){const status=action==='known'?'known':'review';emit({event_type:action==='known'?'flashcard_known':'flashcard_review',scene_id:sceneId(flash),state:{component_state:{flashcard:{status}}})}else{back.hidden=!back.hidden;front.hidden=!front.hidden;emit({event_type:'flashcard_flipped',scene_id:sceneId(flash),state:{component_state:{flashcard:{status:back.hidden?'front':'back'}}})}}return}const match=e.target.closest('[data-match]');if(match){const box=match.closest('[data-matching]'),side=match.dataset.match,pair=match.dataset.pairId,selected=box.querySelector('[data-match][data-selected="true"]');if(selected&&selected.dataset.match!==side){const same=selected.dataset.pairId===pair;selected.dataset.selected='false';match.dataset.selected='false';if(same){selected.dataset.matched='true';match.dataset.matched='true'}const matched=[...box.querySelectorAll('[data-match][data-matched="true"]')].filter(button=>button.dataset.match==='left').map(button=>button.dataset.pairId),complete=matched.length===Number(box.dataset.pairCount||0),feedback=box.querySelector('.feedback');feedback.hidden=false;feedback.textContent=same?(complete?'配对全部正确。':'配对正确。'):'这组配对不一致，请再试一次。';emit({event_type:'matching_attempt',scene_id:sceneId(box),state:{attempt:1,correct:same,component_state:{matching:{matched_pair_ids:matched,completed:complete}}}})}else{box.querySelectorAll('[data-match][data-selected="true"]').forEach(item=>item.dataset.selected='false');match.dataset.selected='true'}return}const move=e.target.closest('[data-order-move]');if(move){const item=move.closest('li'),list=item?.parentElement;if(item&&list){if(move.dataset.orderMove==='up'&&item.previousElementSibling)list.insertBefore(item,item.previousElementSibling);if(move.dataset.orderMove==='down'&&item.nextElementSibling)list.insertBefore(item.nextElementSibling,item)}return}const order=e.target.closest('[data-order-submit]');if(order){const box=order.closest('[data-ordering]'),actual=[...box.querySelectorAll('li')].map(x=>x.dataset.itemId).join('|'),correct=actual===box.dataset.correctOrder,feedback=box.querySelector('.feedback');feedback.hidden=false;feedback.textContent=correct?'排序正确。':'请调整顺序后重试。';emit({event_type:'ordering_submitted',scene_id:sceneId(box),state:{correct,attempt:1,component_state:{ordering:{order:[...box.querySelectorAll('li')].map(x=>x.dataset.itemId),submitted:true,correct}}}});send('ordering_result',{correct})}});
  document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();show(i-1);return}if(e.key==='ArrowRight'){e.preventDefault();show(i+1);return}if((e.key==='Enter'||e.key===' ')&&e.target.matches('[data-flashcard]')){e.preventDefault();e.target.click()}});
  window.addEventListener('message',e=>{if(e.source!==parent)return;if(e.data?.type==='courseware-init'&&typeof e.data.nonce==='string'&&e.data.nonce&&e.data.resource_id===context.resource_id&&e.data.release_id===context.release_id&&e.origin&&e.origin!=='null'){nonce=e.data.nonce;parentOrigin=e.origin;applyRestore(e.data.restore);send('progress',{scene_index:i,scene_count:scenes.length})}if(e.data?.type==='courseware-command'&&e.data?.command==='restart'&&e.data?.nonce===nonce&&nonce)reset()});
  show(0)
})();
""".strip()

# Keep the generated runtime readable enough to lint with a JavaScript parser.
# The raw string above is retained as the browser contract, while this bounded
# handler replacement avoids a single deeply nested click expression.
_runtime_lines = SCRIPT.splitlines()
_runtime_lines[12] = """  const submitQuiz=quiz=>{const selected=[...quiz.querySelectorAll('input:checked')].map(item=>item.value).sort().join('|'),correct=selected===quiz.dataset.answer,feedback=quiz.querySelector('.feedback');if(feedback){feedback.hidden=false;feedback.textContent=correct?'回答正确。':(quiz.dataset.feedback||'请再回顾相关内容后重试。')}emit({event_type:'answer_submitted',scene_id:sceneId(quiz),state:{correct}});emit({event_type:correct?'answer_correct':'answer_retry',scene_id:sceneId(quiz),state:{correct}});send('quiz_result',{correct})};
  const toggleFlash=(flash,target)=>{const action=target?.dataset.flashcardAction,back=flash.querySelector('.flash-back'),front=flash.querySelector('.flash-front');if(action){const status=action==='known'?'known':'review';emit({event_type:action==='known'?'flashcard_known':'flashcard_review',scene_id:sceneId(flash),state:{component_state:{flashcard:{status}}}})}else{back.hidden=!back.hidden;front.hidden=!front.hidden;emit({event_type:'flashcard_flipped',scene_id:sceneId(flash),state:{component_state:{flashcard:{status:back.hidden?'front':'back'}}}})}};
  const submitMatch=match=>{const box=match.closest('[data-matching]'),side=match.dataset.match,pair=match.dataset.pairId,selected=box.querySelector('[data-match][data-selected="true"]');if(selected&&selected.dataset.match!==side){const same=selected.dataset.pairId===pair;selected.dataset.selected='false';match.dataset.selected='false';if(same){selected.dataset.matched='true';match.dataset.matched='true'}const matched=[...box.querySelectorAll('[data-match][data-matched="true"]')].filter(item=>item.dataset.match==='left').map(item=>item.dataset.pairId),complete=matched.length===Number(box.dataset.pairCount||0),feedback=box.querySelector('.feedback');feedback.hidden=false;feedback.textContent=same?(complete?'配对全部正确。':'配对正确。'):'这组配对不一致，请再试一次。';emit({event_type:'matching_attempt',scene_id:sceneId(box),state:{attempt:1,correct:same,component_state:{matching:{matched_pair_ids:matched,completed:complete}}}})}else{box.querySelectorAll('[data-match][data-selected="true"]').forEach(item=>item.dataset.selected='false');match.dataset.selected='true'}};
  const submitOrder=order=>{const box=order.closest('[data-ordering]'),items=[...box.querySelectorAll('li')],actual=items.map(item=>item.dataset.itemId).join('|'),correct=actual===box.dataset.correctOrder,feedback=box.querySelector('.feedback');feedback.hidden=false;feedback.textContent=correct?'排序正确。':'请调整顺序后重试。';emit({event_type:'ordering_submitted',scene_id:sceneId(box),state:{correct,attempt:1,component_state:{ordering:{order:items.map(item=>item.dataset.itemId),submitted:true,correct}}}});send('ordering_result',{correct})};
  document.addEventListener('click',e=>{const n=e.target.closest('[data-nav]');if(n)return show(i+Number(n.dataset.nav));const quiz=e.target.closest('[data-quiz]');if(quiz)return submitQuiz(quiz);const flash=e.target.closest('[data-flashcard]');if(flash)return toggleFlash(flash,e.target);const match=e.target.closest('[data-match]');if(match)return submitMatch(match);const move=e.target.closest('[data-order-move]');if(move){const item=move.closest('li'),list=item?.parentElement;if(item&&list){if(move.dataset.orderMove==='up'&&item.previousElementSibling)list.insertBefore(item,item.previousElementSibling);if(move.dataset.orderMove==='down'&&item.nextElementSibling)list.insertBefore(item.nextElementSibling,item)}return}const order=e.target.closest('[data-order-submit]');if(order)return submitOrder(order)});"""
SCRIPT = "\n".join(_runtime_lines)


def _replace_runtime_const(source: str, name: str, body: str) -> str:
    marker = f"  const {name}="
    start = source.index(marker)
    end = source.index("  const ", start + len(marker))
    return source[:start] + marker + body + source[end:]


# Address component state by scene and stable component instance. This is
# applied to the final readable runtime after the bounded handler replacement.
SCRIPT = _replace_runtime_const(
    SCRIPT,
    "safeState",
    "(state,scene_id,component_id)=>{const result=Object.fromEntries(Object.entries(state||{}).filter(([key])=>['scene_index','scene_count','correct','completed','attempt','duration_ms'].includes(key)));const source=state?.component_state||{},component_state={};if(['front','back','known','review'].includes(source.flashcard?.status))component_state.flashcard={status:source.flashcard.status};if(source.matching&&Array.isArray(source.matching.matched_pair_ids)&&source.matching.matched_pair_ids.every(item=>typeof item==='string'))component_state.matching={matched_pair_ids:[...new Set(source.matching.matched_pair_ids)].slice(0,16),...(typeof source.matching.completed==='boolean'?{completed:source.matching.completed}:{})};if(source.ordering&&Array.isArray(source.ordering.order)&&source.ordering.order.every(item=>typeof item==='string'))component_state.ordering={order:[...new Set(source.ordering.order)].slice(0,16),...(typeof source.ordering.submitted==='boolean'?{submitted:source.ordering.submitted}:{}),...(typeof source.ordering.correct==='boolean'?{correct:source.ordering.correct}:{})};if(Object.keys(component_state).length&&scene_id&&component_id)result.component_state={[scene_id]:{[component_id]:{component_version:'1.0',value:component_state}}};return result};",
)

# v2 interactions use the same bounded event envelope and instance-scoped
# restore contract as the original catalog.  No model content is executable.
SCRIPT = SCRIPT.replace("\n})();", """
  const v2State={};
  document.querySelectorAll('[data-branching]').forEach(box=>{const start=box.dataset.currentNode;box.querySelector(`[data-branch-node="${CSS.escape(start)}"]`)?.removeAttribute('hidden')});
  document.addEventListener('click',e=>{const branch=e.target.closest('[data-branch-option]');if(branch){const box=branch.closest('[data-branching]'),next=branch.dataset.nextNode;box.dataset.currentNode=next;box.querySelectorAll('[data-branch-node]').forEach(node=>{node.hidden=node.dataset.branchNode!==next});const key=box.dataset.componentId;v2State[key]={branch:{node_id:next,option_id:branch.dataset.optionId}};emit({event_type:'branching_attempt',scene_id:sceneId(box),component_id:key,state:{component_state:v2State}});return}const category=e.target.closest('[data-category-id]');if(category){const box=category.closest('[data-categorization]');box.dataset.activeCategory=category.dataset.categoryId;return}const item=e.target.closest('[data-category-item]');if(item){const box=item.closest('[data-categorization]'),correct=item.dataset.correctCategory===box.dataset.activeCategory;item.dataset.correct=String(correct);v2State[box.dataset.componentId]={categorization:{item_id:item.dataset.itemId,category_id:box.dataset.activeCategory,correct}};emit({event_type:'categorization_attempt',scene_id:sceneId(box),component_id:box.dataset.componentId,state:{correct,component_state:v2State}});return}const token=e.target.closest('[data-cloze-token]');if(token){const box=token.closest('[data-word-bank-cloze]'),blank=box.querySelector('[data-cloze-blank]:not([data-filled])');if(blank){blank.textContent=token.textContent;blank.dataset.filled='true';blank.dataset.tokenId=token.dataset.clozeToken;v2State[box.dataset.componentId]={cloze:{blank_id:blank.dataset.clozeBlank,token_id:token.dataset.clozeToken}};emit({event_type:'cloze_submitted',scene_id:sceneId(box),component_id:box.dataset.componentId,state:{component_state:v2State}})}return}const timeline=e.target.closest('[data-timeline-event]');if(timeline){const box=timeline.closest('[data-timeline-explorer]');const detail=box.querySelector('[data-timeline-detail]');if(detail)detail.textContent=timeline.textContent;v2State[box.dataset.componentId]={timeline:{event_id:timeline.dataset.timelineEvent}};emit({event_type:'timeline_selected',scene_id:sceneId(box),component_id:box.dataset.componentId,state:{component_state:v2State}})}});
})();""")
SCRIPT = SCRIPT.replace("state:safeState(event.state)", "state:safeState(event.state,event.scene_id,event.component_id)")
SCRIPT = SCRIPT.replace("scene_id:sceneId(flash),state:", "scene_id:sceneId(flash),component_id:flash.dataset.componentId,state:")
SCRIPT = SCRIPT.replace("scene_id:sceneId(box),state:", "scene_id:sceneId(box),component_id:box.dataset.componentId,state:")
SCRIPT = SCRIPT.replace("parent.postMessage({type,nonce,...extra},parentOrigin)", "parent.postMessage({type,nonce,...context,...extra},parentOrigin)")
SCRIPT = _replace_runtime_const(
    SCRIPT,
    "applyRestore",
    "restore=>{if(!restore||typeof restore!=='object')return;const targetId=typeof restore.current_scene_id==='string'?restore.current_scene_id:'';const targetIndex=Number.isInteger(restore.current_scene_index)?restore.current_scene_index:scenes.findIndex(scene=>scene.dataset.sceneId===targetId);if(targetIndex>=0)show(targetIndex);const instances=restore.component_state?.[targetId]||{};Object.entries(instances).forEach(([componentId,entry])=>{const node=[...document.querySelectorAll('[data-component-id]')].find(item=>item.dataset.componentId===componentId&&item.dataset.sceneId===targetId);const state=entry?.value||{};if(!node)return;const flash=state.flashcard?.status;if(node.matches('[data-flashcard]')&&['front','back','known','review'].includes(flash)){const front=node.querySelector('.flash-front'),back=node.querySelector('.flash-back');if(front)front.hidden=flash==='back';if(back)back.hidden=flash!=='back';node.dataset.reviewStatus=flash}if(node.matches('[data-ordering]')&&Array.isArray(state.ordering?.order)){const list=node.querySelector('ol'),byId=new Map([...node.querySelectorAll('li')].map(item=>[item.dataset.itemId,item]));state.ordering.order.forEach(id=>{const item=byId.get(id);if(item)list.appendChild(item)})}if(node.matches('[data-matching]')&&Array.isArray(state.matching?.matched_pair_ids)){node.querySelectorAll('[data-pair-id]').forEach(button=>{button.dataset.matched=String(state.matching.matched_pair_ids.includes(button.dataset.pairId))})}});};",
)

# Practice workspaces remain simple native checklists, but every change is an
# auditable learning event.  This keeps the interaction reliable offline and
# avoids adding model-controlled runtime behavior.
SCRIPT = SCRIPT.replace("\n})();", """
  document.addEventListener('change',e=>{const step=e.target.closest('[data-check]');if(!step)return;const box=step.closest('[data-component-id]'),checks=[...box.querySelectorAll('[data-check]')],completed=checks.length>0&&checks.every(item=>item.checked);emit({event_type:'practice_step_toggled',scene_id:sceneId(box),component_id:box.dataset.componentId,state:{completed,attempt:checks.filter(item=>item.checked).length}})});
})();""")
