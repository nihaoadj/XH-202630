fastapi==0.115.0
uvicorn[standard]==0.32.0
# Uvicorn 0.32 uses the pre-14 WebSocket protocol modules.  Newer
# websockets releases emit deprecation warnings for that compatibility path.
websockets>=10.4,<14
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
sqlalchemy==2.0.35
langchain==0.3.7
langchain-community==0.3.5
langchain-openai==0.2.6
langgraph==0.2.45
chromadb==0.5.20
sentence-transformers==3.2.1
huggingface-hub==0.36.2
pypdf==5.1.0
pdfplumber==0.11.4
pytest==8.3.3
# Required by the async report-stream integration test; declare it so a clean
# CI runner collects the test suite the same way as local development.
pytest-asyncio==0.24.0
httpx==0.27.2
dependency-injector==4.42.0
pwdlib[argon2]==0.3.0
PyJWT==2.10.1
